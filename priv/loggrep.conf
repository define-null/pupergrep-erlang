{logs,
 [
  %% Можно указать отдельный файл
  {"somelog", "/var/log/syslog"},
  %% Можно указать пачку файлов
  {"somelog2", {files,
                [
                 "/var/log/syslog",
                 "/var/log/mail.log"
                ]}},
  %% Можно указать директорию
  {"agregate_log", "/var/log"}
 ]
}.
